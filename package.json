{
  "name": "nexo-gestao-financeira",
  "private": true,
  "scripts": {
    "build": "node scripts/build-frontend.js",
    "dev": "node scripts/dev.js",
    "postinstall": "node scripts/build-frontend.js --ci --if-missing",
    "heroku-postbuild": "node scripts/build-frontend.js --ci",
    "prepare": "husky"
  },
  "engines": {
    "node": "24.x",
    "npm": "11.x"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  }
}
